class Log:

    def __init__(self, id, user_id, _type, ip, port, url, date, status, user_agent, host):
        self.id = id
        self.user_id = user_id
        self.type = _type
        self.ip = ip
        self.port = port
        self.url = url
        self.date = date
        self.status = status
        self.user_agent = user_agent
        self.host = host